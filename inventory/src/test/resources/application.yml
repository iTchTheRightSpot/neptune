PROFILE: test

spring:
  profiles:
    active: ${PROFILE}
  datasource:
    url: jdbc:postgresql://localhost:5432/neptune_db?sslmode=disable
    username: neptune
    password: neptune

route:
  prefix: api/v1/